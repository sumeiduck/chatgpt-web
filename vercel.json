{
  "routes": [
    { "src": "/api/(.*)", "dest": "https://1485847.1314528.xyz/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "builds": [
    {
      "src": ".",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "pnpm build",
        "outputDirectory": "dist/"
      }
    }
  ]
}
